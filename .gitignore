# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
cloud-fitter

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt

# Dependency directories (remove the comment below to include it)
vendor/

# buf.lock 建议纳入版本库，Docker/CI 可少一步解析且版本可复现；若忽略请确保 Dockerfile 内保留 buf mod update

# idea project
.idea/
.vscode/

# go.sum 应纳入版本库，保证依赖校验与可复现构建（勿再忽略）

# config file
config.yaml


# Compose 挂载的持久化目录（含 SQLite cloud-fitter.db）
data/

log/

# Python
__pycache__/
*.py[cod]

# 前端源码目录 cloud-fitter-web/ 与后端同库提交（业务代码），勿使用仅引用/子模块而不含文件
# 以下仅为构建产物与依赖，勿提交
cloud-fitter-web/node_modules/
cloud-fitter-web/dist/
cloud-fitter-web/.umi/
cloud-fitter-web/.umi-production/
cloud-fitter-web/.mfsu/
cloud-fitter-web/.cache/
cloud-fitter-web/src/.umi/