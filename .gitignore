# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
cloud-fitter

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt

# Dependency directories (remove the comment below to include it)
vendor/

# buf for protobuf
buf.lock

# idea project
.idea/
.vscode/

# go.sum 应纳入版本库，保证依赖校验与可复现构建（勿再忽略）

# config file
config.yaml

log/

# 可选：在仓库根目录克隆前端以便 compose 默认路径可用（勿提交子仓库内容）
cloud-fitter-web/